# Optional proxy and webhook MCP exposure — 2026-08-03

Status: complete
Classification: Full

## Original request

Explain why proxy/webhook operations are not separate optional MCPs and design separate, explicitly enabled exposure in Hub. Then implement the selected virtual-MCP model, fix the Custom GPT OpenAPI importer errors and documentation, and diagnose the current local `/mcp` Bearer/OAuth failure. Issue short-lived public legacy-Hub canary tokens and delete them after the check.

## Objective

Produce an evidence-backed architecture decision for optional virtual `network-proxy` and `webhooks` MCP servers created inside Hub, disabled by default and exposed through normal per-server MCP/Action routes when enabled.

## Goal accepted 2026-08-03

Deliver a new build after real Mac BrowserOS Custom GPT Bearer and OAuth acceptance, schema compatibility repair, optional virtual MCP implementation, and documentation update.

## Business canary

A default Custom GPT schema excludes proxy/webhook operations; enabling one approved capability exposes only its intended schema and endpoints.

## Confirmed scope

Implement virtual MCPs `network-proxy` and `webhooks`, default-off with persisted Admin-Hub enabling; remove their operations from the default Custom GPT schema; update documentation; test locally and diagnose the specified public/local OAuth/Bearer paths. Public legacy-Hub token creation and deletion is authorized only for short-lived canaries.

Also replace the new-install shared `MCP_RELAY_AGENT_TOKEN` setup flow: users must know only `ADMIN_PASSWORD`; ShellMCP enrollment must use a local device identity and Hub-side pending-device approval. Retain the legacy token only as a migration compatibility path for existing installations.

2026-08-03 release expansion: publish the finished build after re-preflight; remove the existing GPTAdmin install on the Mac mini and reinstall it as the normal unprivileged user through SSH; prove a user-mode Linux install in an isolated container. Run an independent read-only review of Custom-GPT CLI/API test gaps and a consolidation proposal for legacy and React admin surfaces. Documentation may follow publication but must be included before final completion.

2026-08-03 acceptance expansion: replace the manual ChatGPT editor check with a reproducible Playwright gate driven through BrowserOS on the Mac mini (not the user's M1). The gate must validate a disposable external Hub contract URL, OpenAPI import without importer warnings, Bearer discovery, and OAuth Authorization Code + PKCE discovery. Publish only reviewed code and test assets to the existing private-origin workflow; credentials and production Hub URLs are excluded from test artifacts.

## Explicit exclusions

No unrelated service changes, deployment/restart, permanent public credentials, or arbitrary tool execution through the canary.

No mutation by the independent review. No release until the requested product canaries and the mandatory release review gates pass.

## Initial estimate (immutable)

- Optimistic: 35 active minutes
- Likely: 75 active minutes
- Pessimistic: 150 active minutes

## План (русский)

1. Сопоставить существующие internal MCP tools, REST/OpenAPI и policy-gates proxy/webhook.
2. Спроектировать virtual MCP registry entries `network-proxy` и `webhooks`, создаваемые только при явном enable.
3. Определить обратную совместимость REST control-plane и default-off Action exposure.
4. Дождаться выбора человека до реализации.

## Progress (English)

- 2026-08-03: architecture reconnaissance started; no implementation authorized.
- 2026-08-03: user corrected the target architecture: do not publish category-specific standalone schemas. Register two optional virtual MCP servers inside Hub (`network-proxy`, `webhooks`) on enable; use normal `/server/{slug}/mcp` and `/server/{slug}/actions/openapi.yaml` surfaces. Assumed the repeated word `proxy` means `webhooks` from the immediately preceding context.
- 2026-08-03: user selected implementation, expanded scope to Custom GPT schema/docs and local/public auth diagnosis. Production mutations limited to issuing and deleting short-lived legacy-Hub canary tokens as explicitly authorized.
- 2026-08-03: live diagnosis: `127.0.0.1:9000` is owned by BrowserOS, not GPTAdmin; the active local Hub is `127.0.0.1:9001` and its unauthenticated `/mcp` returns expected 401. Public `u-f110…/admin/legacy/` login succeeded.
- 2026-08-03: live canary token issuance deliberately paused: current admin API implements only `/admin/api/mcp/tokens/{id}/rotate`, which revokes the old credential while creating a replacement. It has no delete/revoke-without-replacement operation, so issuing a token would violate the explicitly required cleanup.
- 2026-08-03: user demonstrated the authoritative `Clients and Auth` UI revoke control, correcting the narrow token-API finding. Mac BrowserOS Custom GPT editor was configured with API key/Bearer and the real `discover` Action completed successfully, returning 31 servers. Bearer Custom GPT E2E is confirmed; OAuth, code changes, build, and cleanup remain.
- 2026-08-03: user authorized release, publication, a clean per-user Mac mini reinstall, and a Linux container user-install proof. SSH preflight to `roomhacker@192.168.2.8` failed public-key/password authentication, so no Mac mutation has occurred; alternate documented SSH route will be rechecked before asking for intervention.
- 2026-08-03: user corrected the Mac mini address to `192.168.2.4`. The previously reached `localhost:2222` host is excluded from this deployment; the Mac mini will be fingerprint-preflighted and verified independently before any change.
- 2026-08-03: user authorized publication after preflight, requested Mac Mini migration to a user installation and Docker Ubuntu user-install validation, and requested an independent CLI/API and dual-admin-surface coverage review. Mac preflight found the current system CLI at `/usr/local/bin/gptadmin` and legacy per-user `~/.gptadmin`; removal is deferred until a replacement artifact is verified.
- 2026-08-03: reviewed 19 older foreign LHC/workflow changes (664 insertions, 328 deletions). `git diff --check` passed; the changes update LHC version, roles, protocols, task templates, self-improvement records, AGENTS/CLAUDE and roadmap, and remove obsolete kanban/orchestrator templates. User explicitly authorized inclusion in the next build.
- 2026-08-03: user confirmed removal of the new-install shared relay-token UX. New agent enrollment will be identity/approval based; old relay-token support is migration-only.
- 2026-08-03: implemented default-off persisted `network-proxy` and `webhooks` virtual MCPs, each with normal dedicated MCP/Actions routes and an authenticated Hub control endpoint. Both admin surfaces now call the same typed control API; the default Custom GPT OpenAPI has exactly one Bearer scheme and excludes proxy/webhook operations.
- 2026-08-03: Docker acceptance initially found a production bootstrap defect: the curl-installed CLI imported optional `cryptography` before setup. Deferred that optional catalog-only dependency, added a stock-Python regression, and reran the disposable Ubuntu Docker E2E successfully for user/public-hub and sudo/FRP installation paths.
- 2026-08-03: external-process contract test reproduced CLI-issued Bearer 401 against a real Hub. The token lacked the Hub-required `kid`; CLI now emits the configured key id (or canonical default), including audience/resource. The process contract passes after the fix. Focused final validation: 30 Python tests, complete `go test ./internal/hub`, React test/build, Docker E2E, and `git diff --check` all pass.
- 2026-08-03: Mac mini target revalidated as `192.168.2.4` / `Macmini6,2` / `roomhacker`. With explicit operator sudo authorization, stopped and removed only the old system GPTAdmin paths and LaunchDaemons (`/opt/gptadmin`, `/etc/gptadmin`, `/usr/local/bin/gptadmin`, `/Library/LaunchDaemons/com.gptadmin*.plist`); verification confirmed they are absent and the pre-existing user-mode tree remains. Final tagged-artifact user reinstall is still pending.
- 2026-08-03: created narrow commits `2dc2d93`, `553d781`, and `d4eb5b6`; all exclude unrelated LHC/task changes. Tagged local canary `v142` now points to `d4eb5b6`. The tagged build initially exposed and fixed two installer/build defects: stale nested foreign build trees are no longer traversed by tagged cleanup, and macOS user install tries a `user/<uid>` launchd domain after `gui/<uid>`. Build `142` manifest/SBOM verified against `d4eb5b6`.
- 2026-08-03: deployed the Darwin amd64 `v142` archive into `/Users/roomhacker/.local/share/gptadmin` on `192.168.2.4`; binaries are Mach-O x86_64 and the Hub reports build `142` / commit `d4eb5b6` on loopback and through its live FRP public URL. The host has no interactive GUI launchd domain, so macOS refuses user LaunchAgent bootstrap from SSH (125/EIO); Hub/FRP are running as `roomhacker` for the canary and the user LaunchAgent plists remain installed for a GUI-login session.
- 2026-08-03: public-canary OAuth Authorization Code + PKCE proof succeeded without exposing credentials: ChatGPT-compatible redirect accepted, code exchanged at `/oauth/token`, and the returned access token completed MCP `discover`. BrowserOS opened a new ChatGPT editor page but it rendered blank in its current browser session, so the final editor-side OAuth click-through remains unproven.
- 2026-08-03: the newly installed Mac user CLI issued a readonly `issue-token` JWT and the live public Mac Hub accepted it at `/mcp-relay/servers` with HTTP 200. The test JWT was not saved or printed, but legacy stateless JWT issuance has no per-token revoke record; rotating the Hub OAuth signing secret would invalidate it together with any other existing bearer credentials, so no disruptive rotation was performed without a separate operator decision.
- 2026-08-03: replaced the new-install shared relay-token design. `POST /mcp-relay/register` accepts the Hub administrator password only for enrollment, returns a fresh agent-bound credential once, and persists only its SHA-256 digest. Poll/result require the credential for the exact `agent_id`; the old `MCP_RELAY_AGENT_TOKEN` remains a legacy migration path. Generic and macOS Chrome relays atomically replace the one-time password in a `0600` token file and omit the issued value from output. Focused Python and Go regression suites pass.
- 2026-08-03: Critic returned STOP before release: the first relay revision lacked a signed device identity, pending approval and durable-failure handling. Release/tag/publication are paused. The protocol was revised locally to persist an Ed25519 enrollment challenge, require a matching signed proof, reject agent-id takeover, hold the agent in `awaiting_approval`, and issue the credential only after explicit approval and durable state save. Focused Go/Python suites pass; macOS Chrome client identity support and an end-to-end approval canary remain before publication.
- 2026-08-03: user corrected browser ownership: use the Mac mini BrowserOS for the editor gate, not their mostly-offline M1. Initial SSH inspection found the user-mode Hub on `127.0.0.1:9001`, but no BrowserOS listener/process on the usual `9000`, `9002`, or `9101` ports; next work is limited to locating its installed owner/configuration or provisioning an isolated test BrowserOS under the same user without touching the M1.
- 2026-08-03: the actual Mac mini has no BrowserOS installation/listener. User then authorized use of their normally-offline M1 as the browser executor. Added `tests/test_custom_gpt_browser_contract.py`: a disposable real Hub schema gate plus readonly Bearer discovery and a Playwright-driven OAuth Code+PKCE browser flow. It passed locally and through an ephemeral authenticated CDP tunnel to the M1 against a disposable Hub listener reachable at the server-100 LAN address; no personal ChatGPT draft, persistent browser setting, production URL, or credential was modified.
- 2026-08-04: resumed under explicit autonomous-completion instruction. Replaced the conflicting temporary FRP topology with one direct client after unloading its temporary LaunchAgent; the FRP server reported proxy start success. Added Hub-wide secret-safe ingress audit records (`request_id`, method, path, status, Authorization presence) and the CLI `auth-diagnose` contract command (config paths, origin/resource expectations, signing-key fingerprint, redacted claim comparison and distinct local verdicts). Focused Hub and browser contract tests pass. The remaining acceptance work is to obtain one Hub-side ingress record from the Custom GPT invocation, then repeat OAuth and release.
- 2026-08-04: rebuilt the isolated M1 Hub with the canonical URL contract. A token emitted by the actual `gptadmin issue-token --readonly --no-save` command was accepted by the same Hub at loopback and through its unique FRP public origin (`200` / `200`); both safe ingress records showed `GET /mcp-relay/servers`, status `200`, and only `authorization_present=true`. `auth-diagnose` printed matching normalized issuer/audience/resource and a key fingerprint without a secret or JWT. Added red/green regressions for URL normalization, issuer mismatch and diagnostic secrecy; focused Python, Go and opt-in browser OAuth/PKCE tests pass. Rewrote the Custom GPT Actions instructions with the generated schema URL, Bearer sequence, OAuth endpoints and diagnostic path.
- 2026-08-04: the real ChatGPT editor imported/refreshed the disposable public schema through FRP and showed the outbound `discover` confirmation. Its final user-confirmation control rejects CDP synthetic events, while the SSH M1 session lacks macOS Accessibility permission for a trusted System Events click; no Hub request was claimed from this incomplete UI step. This is tracked as the remaining external UI canary, not inferred from preview text.
- 2026-08-04: original Critic STOP on relay enrollment was rechecked against the newer implementation: source now contains persisted Ed25519 enrollment challenge/proof, `awaiting_approval`, agent-id takeover prevention and persistence rollback; the focused Go/Python enrollment suites pass. A repeat independent Critic gate is pending before any staging/publish action.
- 2026-08-04: used the M1-local BrowserOS MCP (`127.0.0.1:9000`) against the real ChatGPT draft, rather than CDP-only automation. BrowserOS snapshot proved the generated schema is present, has the expected four Action operations and has no importer warning; the `discover` outbound confirmation was present with current refs. BrowserOS click, Enter and Space semantic actions all returned success at the transport layer, but ChatGPT retained the protected confirmation and Hub received no `discover`. macOS System Events is denied Accessibility to the SSH session. Therefore no editor-side call has been inferred or claimed; BrowserOS establishes this as an external trusted-consent boundary, not an ingress/token/schema defect.
- 2026-08-04: user reported the isolated Hub was redeployed with PKCE optional for OAuth clients and requested an immediate repeat of the real M1 Custom GPT OAuth canary, with no repository or code changes. Pending evidence is authorize login, password completion, `/oauth/token` success, and ChatGPT-originated `/mcp-relay/servers` HTTP 2xx.
- 2026-08-04: completed the resumed live M1 Custom GPT OAuth canary without code or deployment changes. The real preview opened the public Hub authorize page with the exact `chat.openai.com/aip/g-.../oauth/callback` and no PKCE, the password form was completed through BrowserOS, and the editor returned with `oauth_success=true`. Safe Hub evidence shows authorize `POST` 302, token `POST` 200, then ChatGPT-User `GET /mcp-relay/servers` 200 with request IDs `89849127afc6cd8bb...`, `0f4e55673cac6119...`, and `73f0eea7579c95d4...`. The preview visibly shows two `[debug] Calling HTTP endpoint` / `[debug] Response received` pairs; no secret, authorization code, or token was exposed.
- 2026-08-04: aligned the operator docs and browser-contract release gate with the observed Custom GPT protocol: Custom GPT uses Authorization Code but omits PKCE; the Hub accepts no-PKCE only for that compatibility path and validates S256 when supplied. Focused Go Hub tests and Python Action/browser-contract tests pass. Release preflight continues with the completed real ChatGPT Bearer and OAuth evidence above.
- 2026-08-04: mandatory Critic pre-release review returned STOP because the first no-PKCE implementation was global. Fixed before any tag/build: the exception now requires the exact `https://chat.openai.com/aip/g-<id>/oauth/callback` profile with one path segment; localhost, connector, arbitrary and malformed OpenAI redirects require S256. Added red/green regression coverage and reran focused plus full `go test ./internal/hub` successfully.
- 2026-08-04: release `v145` was built locally (14 manifest-verified artifacts) and published after GitHub Actions completed every platform, UI, failover, Python, Go, manifest/SBOM, installer-link and dependency-scan gate. Public mirror and non-draft GitHub Release assets were confirmed. Earlier `v143`/`v144` tags remain immutable failed attempts; `v145` is the successful release.
