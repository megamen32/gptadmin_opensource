# Задача: уточнить proxy/webhook виртуальные MCP-возможности и дефолтную схему Custom GPT Actions

Status: complete

## Оригинальный запрос

Переписать/прояснить proxy и webhook как отдельные, явно включаемые виртуальные MCP-возможности: по умолчанию выключены, что каждая даёт, самые простые команды enable/use/check — только если они подтверждаются в исходниках; стиль — короткими one-liner'ами. Также убедиться, что дефолтная схема Custom GPT Actions исключает их.

## Objective

Обновить только:

- `docs/MCP_PROXY_RELAY.md`
- `docs/API_REFERENCE.md`
- `docs/CONFIGURATION.md`

так, чтобы документация однозначно описывала proxy и webhook как separate, opt-in virtual MCP capabilities, а default Custom GPT Actions schema не включала их.

## Business canary

Проверить, что в итоговой документации:

- proxy и webhook описаны как отдельные выключенные по умолчанию capability;
- для каждой capability есть краткое описание ценности;
- команды enable/use/check приведены только при подтверждении в исходниках;
- default schema для Custom GPT Actions не содержит proxy/webhook.

## Подтверждённый scope

- Только редактирование трёх указанных docs-файлов.
- Только текстовые правки в формате коротких one-liner'ов.
- Только команды, которые можно подтвердить по исходникам.
- Затем focused docs/API contract tests.

## Явные исключения

- Не трогать другие файлы.
- Не менять runtime/код, если это не потребуется для документной синхронизации.
- Не добавлять непроверенные команды или поведение.
- Не делать широкий рефакторинг формулировок вне затронутых секций.

## Оценка времени

- optimistic: 15 минут
- likely: 30 минут
- pessimistic: 45 минут

## План

1. Проверить текущие формулировки в указанных docs и подтверждающие места в исходниках.
2. Обновить три docs-файла минимальными правками.
3. Прогнать focused docs/API contract tests.
4. Зафиксировать изменённые пути и точные тесты.
