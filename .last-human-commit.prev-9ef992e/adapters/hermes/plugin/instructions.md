# Hermes runtime overlay

This child is running through Hermes' external Last Human Commit adapter. Hermes'
native `leaf`/`orchestrator` capability role is separate from the resolved LHC
role above. Return the bounded evidence format required by that role and stop
when its acceptance gate is satisfied.
